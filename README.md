# 🏦 APIOrchestra — Автоматизированное тестирование API-оркестров с локальным ИИ

**Автор**: Дмитрий Крылосов, Уфа  
**Квалификация**: Дипломированный Python-разработчик, высшее экономическое образование  
**Язык**: Python 3 (ядро), Java 17 (обёртка)  
**Цель**: Тестировать бизнес-процессы, а не отдельные эндпоинты.

---

## 💡 Проблема

Современные цифровые сервисы — это **цепочки REST API**, объединённые в бизнес-процессы.  
Но их тестирование остаётся **ручным и хрупким**.

---

## 🚀 Решение

**APIOrchestra** — система, которая:
- Принимает **BPMN 2.0** и **OpenAPI 3.0+**
- Автоматически тестирует цепочку вызовов
- Использует **локальную ИИ-модель** для анализа соответствия
- Генерирует тестовые данные
- Возвращает отчёт с рекомендациями

**Особенности**:
- ✅ Ядро на Python — твой рабочий прототип (версия 2.0)
- ✅ **Локальная ИИ-модель** all-MiniLM-L6-v2 — работает без интернета
- ✅ Анализирует соответствие BPMN и OpenAPI
- ✅ Java-обёртка для интеграции и веб-интерфейса
- ✅ **Без GPT, без облака** — всё локально

---

## 🧠 Локальная ИИ-модель

- **Модель**: all-MiniLM-L6-v2 (sentence-transformers)
- **Размер**: ~90 MB
- **Возможности**:
  - Анализирует семантическое соответствие между задачами BPMN и операциями API
  - Оценивает степень соответствия (0.0-1.0)
  - Генерирует рекомендации по улучшению
- **Преимущества**:
  - Работает полностью локально
  - Не требует интернета после установки
  - Подходит для ГОСТ-шлюзов и банковской инфраструктуры

---

## 📁 Структура
      api-orchestra/
      ├── src/main/java/com/apiorchestra/Application.java
      ├── src/main/resources/
      │ ├── application.yml
      │ ├── openapi.yaml # API Virtual Bank
      │ └── process.bpmn # BPMN-диаграмма "Оформление кредита"
      ├── virtual_bank_orchestrator.py # Твой рабочий прототип с локальным ИИ
      ├── requirements.txt # Зависимости для ИИ-модели
      ├── pom.xml
      └── target/api-orchestra-1.0.0.jar

## 🛠️ Как запустить

1. Установи Python 3.8+ и Java 17
2. Установи зависимости для ИИ:
```bash
pip install -r requirements.txt
mvn clean package
java -jar target/api-orchestra-1.0.0.jar
Открой: http://localhost:8080
Загрузи BPMN и OpenAPI файлы → нажми "Запустить тест"


```
---

## 🚀 Как это работает с локальной ИИ-моделью

1. **Ты запускаешь Java-приложение**: `java -jar target/api-orchestra-1.0.0.jar`
2. **Открываешь браузер**: `http://localhost:8080`
3. **Загружаешь** BPMN и OpenAPI файлы
4. **Java** вызывает **твой Python-прототип** (`virtual_bank_orchestrator.py`)
5. **Python**:
   - Загружает **локальную ИИ-модель** all-MiniLM-L6-v2
   - Анализирует соответствие задач BPMN и операций API
   - Выполняет оркестрацию (тестирование цепочки)
   - Генерирует рекомендации на основе ИИ-анализа
6. **Python** возвращает JSON-отчёт с ИИ-анализом
7. **Java** показывает результат в браузере


## 📊 Пример вывода ИИ-анализа
🤖 ИИ-анализ: соответствие 0.92 - Соответствие отличное. Шаг может быть выполнен без изменений.
🤖 ИИ-анализ: соответствие 0.85 - Соответствие хорошее. Возможно небольшое несоответствие в параметрах.
🤖 ИИ-анализ: соответствие 0.43 - Соответствие низкое. Шаг, вероятно, не может быть выполнен с текущими параметрами.

🔍 Проблемы, найденные ИИ-анализом:
🔴 Высокая проблема: Проверить статус платежа ↔ Проверить статус платежа
Соответствие: 0.43 — Соответствие низкое. Шаг, вероятно, не может быть выполнен с текущими параметрами.

